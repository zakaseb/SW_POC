# --- LM Studio server (OpenAI-compatible) ---
OPENAI_BASE_URL=http://127.0.0.1:1234/v1
OPENAI_API_KEY=lm-studio

# --- Model IDs (must match LM Studio /v1/models) ---
OLLAMA_LLM_NAME=mistral-7b-instruct-v0.3
OLLAMA_EMBEDDING_MODEL_NAME=bge-m3

# --- Optional local reranker (SentenceTransformers, not LM Studio) ---
RERANKER_MODEL_NAME=cross-encoder/ms-marco-MiniLM-L-6-v2

# --- Paths (these should exist or be creatable) ---
PDF_STORAGE_PATH=document_store/pdfs/
CONTEXT_PDF_STORAGE_PATH=document_store/context_pdfs/
MEMORY_FILE_PATH=document_store/memory/context.json
